# Install alongside requirements.txt:
#   pip install -r requirements.txt -r requirements-dev.txt

pytest>=8.1.0
pytest-asyncio>=0.23.0
anyio>=4.3.0
httpx>=0.27.0        # already in requirements.txt — included here for visibility
